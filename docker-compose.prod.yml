# Production override for docker-compose
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

version: "3.9"

services:
  api:
    image: ghcr.io/YOUR_USERNAME/adhd-board-api:latest
    build:
      # Remove build config - use pre-built image
      context: null
      dockerfile: null

  frontend:
    image: ghcr.io/YOUR_USERNAME/adhd-board-frontend:latest
    build:
      # Remove build config - use pre-built image
      context: null
      dockerfile: null
      args: null
